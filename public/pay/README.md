Drop the PhonePe merchant QR image here as `phonepe-qr.png`.

It is shown on the checkout page when a customer is blocked for an
overdue payment (see `src/components/PaymentDueBlock.tsx`). Any UPI app can
scan a PhonePe merchant QR. Keep the file under ~300 KB; a screenshot of
the "Accepted here" poster is fine.
